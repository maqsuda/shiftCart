## WELCOME TO ( সহজ সরল সিম্পল ) ASSIGNMENT (SwiftCart E-Commerce)

#### 1) What is the difference between `null` and `undefined`?

undefine is value not initialize.
null is define but absence of value.

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?

forEach() does not return but map() return an array.

#### 3) What is the difference between `==` and `===`?

== is compare value and === compare value and data type.

#### 4) What is the significance of `async`/`await` in fetching API data?

#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).

Global is accessed by any function, block.
block is accessed within currly bracket.
Function is accessed within function.

## ⚡ Dynamic Features & Functionalities
